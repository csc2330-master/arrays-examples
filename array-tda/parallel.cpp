#include <iostream>
#include <iomanip>
#include <string>

using std::cout;
using std::cin;
using std::endl;
using std::setw;
using std::string;
using std::ostream;


bool Insert(string names[], int ids[], size_t& length, size_t capacity, size_t index, const string& name, int id);
bool Remove(string names[], int ids[], size_t& length, int idToRemove);
void WriteList(const string names[], const int ids[],  size_t length, ostream& output);
size_t ReadList(string list[], int ids[], size_t& length, istream& input);
int FindName(const string names[], const int ids[],  size_t length, int id);

int main(int argc, char* argv[]){


	return 0;
}


bool Insert(string names[], int ids[], size_t& length, size_t capacity, size_t index, const string& name, int id){

}
bool Remove(string names[], int ids[], size_t& length, int idToRemove){

}
void WriteList(const string names[], const int ids[],  size_t length, ostream& output){

}
size_t ReadList(string list[], int ids[], size_t& length, istream& input){

}
int FindName(const string names[], const int ids[],  size_t length, int id){

}
