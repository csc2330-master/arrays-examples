#include <iostream>
#include <iomanip>
#include <string>

using std::cout;
using std::cin;
using std::endl;
using std::setw;
using std::string;
using std::ostream;


bool Insert(string list[], size_t& length, size_t capacity, size_t index, const string& value);
bool Remove(string list[], size_t& length, size_t index);
void WriteList(const string list[], size_t length, ostream& output);
size_t ReadList(string list[], size_t& length, istream& input);
int Find(const string[] list, size_t length, const string& key);

int main(int argc, char* argv[]){


	return 0;
}


bool Insert(string list[], size_t& length, size_t capacity, size_t index, const string& value){

}
bool Remove(string list[], size_t& length, size_t index){

}
void WriteList(const string list[], size_t length, ostream& output){

}
size_t ReadList(string list[], size_t& length, istream& input){

}
int Find(const string[] list, size_t length, const string& key){

}
